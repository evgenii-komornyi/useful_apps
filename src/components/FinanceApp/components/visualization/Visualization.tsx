import { FC } from 'react';
import { BarsChart } from './components/bar-chart';

export const Visualization: FC = () => {
    return (
        <div>
            <BarsChart />
        </div>
    );
};
