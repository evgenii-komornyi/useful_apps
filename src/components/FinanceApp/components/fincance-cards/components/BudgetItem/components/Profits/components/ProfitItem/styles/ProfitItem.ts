import styled from "styled-components";

export const ProfitItemContainer = styled.div`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    padding: 2px;
    margin: 1px 0;
`

export const ProfitItemCurrencyContainer = styled.div`
    position: relative;
    padding: 1px;
    display: flex;
    flex-direction: row;
`